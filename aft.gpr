project Aft is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("aft_exts.adb");

   package Pretty_Printer is
      for Default_Switches ("ada") use ("-i4", "--no-separate-is");
   end Pretty_Printer;

   package Ide is
      for Vcs_Kind use "git";
   end Ide;

end Aft;

